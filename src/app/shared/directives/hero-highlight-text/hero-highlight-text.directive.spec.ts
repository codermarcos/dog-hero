import { HeroHighlightTextDirective } from './hero-highlight-text.directive';

describe('HeroHighlightTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroHighlightTextDirective();
    expect(directive).toBeTruthy();
  });
});
